// export default defineNuxtRouteMiddleware( async ( to ) => {
//   const { loggedIn } = useUserSession()
//   if ( !loggedIn.value ) {
//     throw createError( {
//       statusCode: 404,
//       message   : "Unauthorized"
//     } )
//   }
// } )
